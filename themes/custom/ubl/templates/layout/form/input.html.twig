{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}
{% if attributes.id=='edit-periode-min' %}
    <input{{ attributes }} placeholder="{{ "Date de début"|t }}" data-date-format="yyyy-mm-dd"/>{{ children }}
{% elseif attributes.id=='edit-periode-max' %}
    <input{{ attributes }} placeholder="{{ "Date de fin"|t }}" data-date-format="yyyy-mm-dd"/>{{ children }}
{% else %}
    <input{{ attributes }} />{{ children }}
{% endif %}
